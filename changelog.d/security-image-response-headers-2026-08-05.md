Harden local image/file responses: `/api/local-image` now sends `Content-Security-Policy: default-src 'none'; sandbox` so a crafted on-disk `.svg` opened as a top-level document can't run script in CCC's origin (rendering via `<img>` is unaffected), and `X-Content-Type-Options: nosniff` is added to the `/api/local-image`, `/image-cache/`, `/api/pasted-image`, and `/api/conv-image` responses to stop MIME sniffing.
