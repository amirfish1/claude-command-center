# CCC Release Notes — 2026-09-03

_Range: `--since 7 days ago`_

## New

### Server
- **What:** CCC becomes a Claude Code peer (slice 3); includes Inbound Claude-peer frame parsing for CCC's own socket; CCC's own Claude-peer registry row and key-file builders; Spawned Claude sessions accept peer messages unattended; /api/ask rides the peer socket before keystrokes; Agent-to-agent injects over Claude peer sockets routes correctly; Peer-socket delivery receipt via transcript msg_id scan; Stdlib client helpers for Claude Code peer sockets; Held peer messages as a muted notice is visible; Tag absorbed-mid-turn peer attachments with sender; Delivered peer messages in the transcript reader is visible. **Why it matters:** answers questions from your own history instead of you digging for them.

**How:** `server.py`, `tests/test_ccc_peer_server.py`, `tests/test_report_to_footer.py`, `ccc_peer_inbound.py`, …

### Kimi
- **What:** Heal poisoned runtime bindings on view; pin the kap daemon; includes Kap flag can be flipped without restarting the server; Transport pill names the transport (KAP vs ACP); Kimi turns over kap when the daemon owns the session routes correctly; Kap steer, session routing, and a live turn pump; Flag-gated render bridge into CCC's conversation store; Kap-server transport spike — discovery, REST, WS, event mapping; Total Recall knowledge bridge is available. **Why it matters:** makes Kimi sessions more reliable inside CCC.

**How:** `ccc_server/kap.py`, `changelog.d/added-kap-binding-heal-on-view-2026-09-03.md`, `changelog.d/added-kap-server-pin-2026-09-03.md`, `server.py`, …

### Ask
- **What:** Classify Ask sessions as Other; includes Confirm-button spawn-continue action in the Ask tab; Spawn-continue action markers (model proposes, UI confirms); Ask tab UI — chat pane with clickable session chips; POST /api/assistant/ask — retrieval + one cheap-LLM call; Pure core for the Ask tab assistant (ccc_server/ask.py). **Why it matters:** answers questions from your own history instead of you digging for them.

**How:** `ccc_server/ask.py`, `changelog.d/added-ask-session-other-lane-2026-08-31.md`, `server.py`, `static/app.js`, …

### Interface
- **What:** + icon for conv attach, moved to left of toolbar row; includes Collapse archived engine filter to single-icon toggle; Mobile file/photo attach button to composer is available; Wrap toggle to Coding/Workers/Other tab toolbar too is available; Wrap toggle to clamp session titles at two lines is available. **Why it matters:** makes it easier to send files and screenshots into a session.

**How:** `changelog.d/added-conv-attach-plus-left-2026-09-03.md`, `static/index.html`, `static/app.css`, `static/app.js`

- **What:** Model-picker favorites on every New-session selection is recorded; includes Quick model picker above composer in new session mode is available; Model picker to new session Stage is available; Fold compact/comfortable + Active look into row-style picker. **Why it matters:** picks the right model with less clicking.

**How:** `changelog.d/changed-model-picker-favorites-always-current-2026-09-02.md`, `static/app.js`, `changelog.d/added-model-picker-new-session-2026-08-29.md`, `server.py`, …

- **What:** Pull to refresh on phone Coding and Workers; includes Phone tabs are Coding, Workers, Queues, q2; Phone Queues tab opens q2; cold open stays on list. **Why it matters:** keeps queues moving without you babysitting them.

**How:** `static/app.css`, `static/app.js`, `static/index.html`, `tests/test_smoke.py`, …

- **What:** World-class styling for Ask / Mazkir assistant rail; includes Normalize status-rail token headline to Opus-5-equivalent cost (CCC-1016); Redesign Ask tab as verdict + results list. **Why it matters:** answers questions from your own history instead of you digging for them.

**How:** `changelog.d/added-ask-mazkir-world-class-styling-2026-09-04.md`, `static/app.css`, `static/app.js`, `static/index.html`, …

### Ask
- **What:** Mazkir — tool-using Sonnet agent over claude-index + ccc-state MCPs (stdlib stdio MCP in ccc_server/mazkir.py); includes Generalize tool-access escalation beyond trigger questions; Escalate to bounded tool access when automation search finds nothing. **Why it matters:** answers questions from your own history instead of you digging for them.

**How:** `ccc_server/ask.py`, `ccc_server/mazkir.py`, `tests/test_mazkir.py`, `tests/test_ask.py`

### BYOK
- **What:** Droid/aider/pi spawn paths, doctor, byok-ready markers is wired in; includes BYOK key storage, profiles, and spawn env injection is available. **Why it matters:** lets each engine run with the provider keys you chose.

**How:** `ccc`, `ccc_server/aider.py`, `ccc_server/droid.py`, `ccc_server/engines.py`, …

### Messaging
- **What:** UDS peer transport for /api/inject-input calls is enabled; includes Default CCC_MESSAGING_BACKEND to uds-on. **Why it matters:** gets agent-to-agent messages delivered with less manual recovery.

**How:** `ccc`, `ccc_server/watchtower_msg.py`, `changelog.d/changed-inject-input-uds-2026-09-02.md`, `server.py`, …

### Queue
- **What:** WatchTower error strip above the queue with ack/dismiss; includes Ack toggle for caveat/unresolved chips (parity w/ WatchTower). **Why it matters:** keeps queues moving without you babysitting them.

**How:** `ccc_server/wt_alerts.py`, `static/app.css`, `static/index.html`, `tests/test_wt_alerts.py`, …

### Models
- **What:** Prune antigravity catalog to latest tiers; includes Claude Fable 5.1; repair Anthropic catalog refresh is available. **Why it matters:** makes model choice easier to compare before you start.

**How:** `changelog.d/changed-antigravity-model-pruning-2026-09-02.md`, `server.py`, `static/app.js`, `changelog.d/added-claude-fable-5-1-catalog-2026-09-01.md`, …

- and 42 more New capabilities.

## Improved

### Github
- **What:** Shared GraphQL quota meter, PR cache and single-flight. **Why it matters:** gives visibility into usage before you hit a limit.

**How:** `ccc`, `ccc_server/cross_repo_issues.py`, `ccc_server/fleet.py`, `ccc_server/github_quota.py`, …

### Mazkir
- **What:** Lite index MCP, overlap prefetch with claude boot, 110-word answers. **Why it matters:** answers questions from your own history instead of you digging for them.

**How:** `ccc_server/mazkir.py`

### Archive
- **What:** Busting persisted cache on every server.py edit no longer happens. **Why it matters:** gets you to old conversations faster.

**How:** `server.py`

## Fixed

### Queue
- **What:** Saving CCC default engine no longer snaps back to claude; includes Writing a hardcoded claude engine into queue config no longer happens; Trust GitHub queue config for badge + surface sync staleness; GitHub rate-limit backoff from cache-poisoning a queue's issues (CCC-1021) no longer happens; Run reconcile_once off-thread after queue config/drain/workers writes; Roll back failed mutations atomically; Mutations explicitly authoritative works correctly; Preserve authoritative FIFO edits; Ticket opens are instant — cached row first, live hydrate after; Context endpoint falls back to the cached list item on gh quota. **Why it matters:** keeps queues moving without you babysitting them.

**How:** `server.py`, `tests/test_smoke.py`, `static/app.js`, `ccc_server/queue_events.py`, …

### Codex
- **What:** Centralize queued delivery in worker; includes Move queued steer transaction to worker; Harden queued steer finalization; Queued steer exactly once works correctly; Defer queued steer ack suppression; Coordinate queued steer acknowledgements; Serialize queued steer claims; Atomic queued input claims is available. **Why it matters:** makes Codex sessions easier to steer and read from CCC.

**How:** `ccc_server/codex.py`, `ccc_server/pending_inputs.py`, `ccc_server/queue_events.py`, `tests/test_codex_queued_steer_once.py`, …

### Kimi
- **What:** Tail yields to an active kap pump (no double renders) is wired in; includes Rebind kap-driven sessions to the local runtime; Kap flag marker lands where a shell can create it; Why a kap turn failed instead of just stopping is visible; Answer kap-server's application-level ping or get dropped at 20s; Set the session model via POST /profile, not create-time agent_config; Pass agent_config.model when creating a kap session; Correct kap event mapping against events-zod.ts. **Why it matters:** makes Kimi sessions more reliable inside CCC.

**How:** `ccc_server/wire_tail.py`, `changelog.d/fixed-kimi-kap-wire-tail-double-fold-2026-09-03.md`, `docs/kimi-kap/runtime-binding.md`, `tests/test_smoke.py`, …

### Interface
- **What:** Antigravity/grok in archived engine-filter click handler is accepted; includes Archive engine filter opens on hover, not click (CCC-1015); Place Grok approval controls in live branch; Grok approvals actionable works correctly; Sidebar poll from flickering hover state on stationary rows no longer happens; Conv list on aborted archive poll stays in place. **Why it matters:** lets you act on blocked agent work from the dashboard.

**How:** `static/app.js`, `changelog.d/fixed-grok-approval-actions-2026-09-01.md`, `tests/test_grok_approval_ui_static.py`, `tests/test_sidebar_window_invariants.py`

- **What:** Move transport pill out of the pane titlebar to the Metadata tab; includes Wrap-mode conv-folder-group-chip pill treatment works correctly; Peer-message green background is overridden by conv-bg theme rules; Peer-message green tint visible over a custom conversation background stays in place; NYA stale-tool age from rebuilding the conv list every minute no longer happens. **Why it matters:** gets agent-to-agent messages delivered with less manual recovery.

**How:** `changelog.d/changed-transport-chip-to-metadata-tab-2026-09-03.md`, `static/app.js`, `static/index.html`, `static/app.css`, …

### Queue
- **What:** Harden metadata sidecar recovery; includes Validate versioned ID sidecar; Harden volatile recovery state; Protect pending claim recovery; Claim recovery idempotent works correctly. **Why it matters:** keeps queues moving without you babysitting them.

**How:** `ccc_server/codex.py`, `ccc_server/pending_inputs.py`, `tests/test_codex_queued_steer_once.py`, `ccc_server/queue_events.py`, …

### Interface
- **What:** Const for _allTabConvs after lane-map refactor is restored; includes Resolve double const declaration crash in static/app.js; Orchestration lane count on conversation rows without selection is visible; Orchestration lane count on conversation rows without selection is visible. **Why it matters:** makes multi-agent work easier to follow from the lane map.

**How:** `static/app.js`, `changelog.d/fixed-double-const-appjs-2026-08-28.md`, `changelog.d/fixed-orch-lane-count-always-visible-2026-08-28.md`, `static/app.css`

### Sidebar
- **What:** Prevent continuation sessions from being routed to workers; includes Fold continuation origin into its successor, chip always visible; Continuation successor labels as successor, lane count rolls up the chain. **Why it matters:** makes related sessions easier to scan and open.

**How:** `changelog.d/fixed-continuation-worker-routing-2026-08-29.md`, `server.py`, `static/app.js`, `tests/test_smoke.py`, …

### Antigravity
- **What:** Headless agy sessions in list and live view is visible; includes Detect live agy resume via process table, not just registry; End-of-turn detection recognizes a hung headless resume. **Why it matters:** makes runaway or stale processes easier to spot and stop.

**How:** `ccc_server/engines.py`, `changelog.d/fixed-antigravity-cli-archive-signature-2026-09-03.md`, `changelog.d/fixed-antigravity-live-view-pre-transcript-2026-09-03.md`, `server.py`, …

### Server
- **What:** Single UDS send, post-send receipt scan, wire announced_from and wt sources; includes Peer socket send never raises on bad inputs; Held peer notice keeps a clean sender when no name clause. **Why it matters:** gets agent-to-agent messages delivered with less manual recovery.

**How:** `server.py`, `tests/test_peer_uds_router.py`, `ccc_peer_uds.py`, `tests/test_ccc_peer_uds.py`, …

### Interface
- **What:** Reconcile pending spawn cards for punctuation-heavy prompts; includes Copy button to pending/queued user messages is available; Grouped-row icon overlap and cards-mode active tint works correctly. **Why it matters:** makes it clearer where a session came from.

**How:** `changelog.d/fixed-pending-spawn-prompt-normalize-2026-09-03.md`, `static/app.js`, `static/app.css`, `changelog.d/fixed-grouped-row-icon-overlap-cards-active-tint-2026-08-28.md`

### Queue
- **What:** Move the All/Open status filter into the ticket filter row; includes Poll-driven ticket-list rebuild from eating mobile taps (CCC-1020) no longer happens; Collapse WORKING NOW strip on mobile queue tab (CCC-1019). **Why it matters:** keeps queues moving without you babysitting them.

**How:** `static/app.css`, `static/index.html`, `static/q2.js`, `static/app.js`

- and 59 more Fixed capabilities.

## Removed

### Interface
- **What:** COO board is gone. **Why it matters:** keeps the dashboard focused on active command-center workflows.

**How:** `changelog.d/removed-coo-board-2026-08-28.md`, `docs/objects-day-view-tracker.md`, `docs/product-story/pain-feature-proof.md`, `objects_store.py`, …

### Queue
- **What:** Redundant per-worker rows from the compact queue status strip (CCC-1019) is gone. **Why it matters:** keeps queues moving without you babysitting them.

**How:** `static/app.css`, `static/app.js`, `tests/test_q2_worker_idle_status.py`

### Lane Map
- **What:** Is-born animation class after entry and stop animations on landed items is gone. **Why it matters:** makes multi-agent work easier to follow from the lane map.

**How:** `static/app.css`, `static/app.js`

### Interface
- **What:** Unused peer chip hook from the assistant render path is gone. **Why it matters:** gets agent-to-agent messages delivered with less manual recovery.

**How:** `static/app.js`

### Sidebar
- **What:** Now-wrong per-cost-tier content-left overrides is gone. **Why it matters:** makes related sessions easier to scan and open.

**How:** `static/app.css`

<details>
<summary>Under the hood (167 commits)</summary>

- `e80dfbf` chore(release): v5.30.0
- `43f58a9` chore(release): v5.28.0
- `11074ee` docs(readme): swap kanban feature-wall rows for queue/worker captures
- `5258974` chore(release): v5.31.0
- `2f958d7` chore(release): v5.29.0
- `e72e7f2` fix(release): align v5.29 launch copy
- `2e68ad6` fix(ui): keep session list mounted when switching to Queues
- `dcaceda` feat(ui): focus session summaries on next action
- `8d11050` feat(release-notes): value-first release notes generator
- `db29645` fix(restart): verify launchd owns this pid before trusting kickstart
- `c4a7cf7` fix(ui): don't block trash on Devin list rebuilds
- `177a5e6` fix(antigravity): bind spawns whose log outgrew the 64KB tail window
- and 155 more under-the-hood commits.

</details>
