# Lane W3-3 — BYOK harness gaps (droid spawn, aider dispatch, Pi engine, doctor)

Updated: 2026-09-03 (done)
Status: DONE — all 6 brief items shipped, tested, verified on isolated PORT=8099
Owner: CCC lane W3-3 (dispatched by 25c52af6-c616-46a2-b47d-0f8a9e016055)

## Scope (brief items 3C + 3D)
1. Real droid spawn path (Factory `droid` CLI) with model + effort passthrough + BYOK env.
2. Wire `aider` into server.py's dispatch chain (spawn_session_aider existed but was dead code).
3. Add a Pi engine (`pi` CLI) — real spawn if a CLI convention can be trusted, else a
   clean "not installed" stub + install docs.
4. `ccc models` / `/api/engines/models` mark BYOK-ready engines.
5. `ccc doctor` (or a health route) — per engine: CLI present, auth present, BYOK
   profile present, 1-token dry-run smoke spawn.
6. README + changelog.d + tests.

## Key facts found
- `droid`, `aider`, `kilo`, `hermes`, `pi` are already in `BYOK_DIRECT_ENV_ENGINES`
  (ccc_server/byok.py) — env injection plumbing (`byok_spawn_env`) is generic,
  only the spawn call sites needed `env=` wiring.
- Neither `droid`, `aider`, nor `pi` CLI is installed on this machine — cannot
  empirically verify real CLI flags. Droid has *some* grounding: `ccc_server/droid.py`
  already shells out to `droid exec --help` to scrape the model catalog, so `exec`
  is a confirmed subcommand; `--model`/`--auto`/`--reasoning-effort` flag names are
  best-effort (documented as such in code comments + README), not verified against
  the real binary.
- Pi has zero grounding in this repo (no prior code, no docs) — implemented as a
  bin-resolver + clean "not installed" stub only, per the brief's explicit fallback
  instruction, not a guessed exec path.
- Aider's spawn path (`ccc_server/aider.py`) was fully implemented but never called
  from server.py's ~18-engine dispatch chain (confirmed via W2-2's own scope note).

## What shipped
1. `ccc_server/droid.py`: real `droid exec --auto high` spawn — model,
   per-model reasoning-effort ladder (`_droid_reasoning_efforts_for_model`,
   drops unsupported efforts instead of forwarding a bad flag), BYOK `env=`.
2. `ccc_server/aider.py` + `server.py`: `env=` threaded through
   `_aider_start_turn`/`spawn_session_aider`; new `elif engine == "aider":`
   branch in the `/api/sessions/spawn` dispatch chain (previously dead code
   — the function existed, nothing called it).
3. `ccc_server/pi.py` (new): bin resolver + a spawn that *always* reports
   `pi_unavailable` ("not yet wired", never launches a subprocess) — no `pi`
   CLI exists anywhere to validate an exec contract against, so this is an
   honest stub, not a guess. Wired into `_ORCHESTRATION_SPAWN_ENGINES`,
   aliases, `_ENGINE_SUPPORTS_CUSTOM_MODELS`, `_adopt_ccc_module`.
4. `_build_engine_model_catalog()` + `ccc models`: every engine bucket now
   carries `byok_ready` (`engine in BYOK_DIRECT_ENV_ENGINES`); CLI prints
   `[BYOK-ready]` in the header line.
5. `build_ccc_doctor()` / `/api/engines/doctor` / `ccc doctor`: per engine —
   `cli_present` (reuses `_engines_installed`), `auth_present`
   (`_doctor_auth_present`: reuses onboarding's `logged_in` for the 5 engines
   it covers + an opencode `auth.json` probe; explicit `None` — never a false
   `False` — for the other 8, tagged `no-probe-implemented`),
   `byok_profile_present`, and `smoke_dry_run` (CLI-resolves-only, never
   spawns, never spends a token).
6. README §BYOK "Current scope" line updated (was stale: said OpenCode-only),
   new README §BYOK "Health check" paragraph, `ccc doctor` line in the CLI
   usage block, two changelog.d snippets
   (`added-byok-droid-aider-pi-2026-09-03.md`, `added-ccc-doctor-2026-09-03.md`).

## Tests
37 new/updated tests, all passing:
`tests/test_droid_engine.py` (4), `tests/test_pi_engine.py` (3),
`tests/test_pi_engine_dispatch.py` (2), `tests/test_engine_doctor.py` (5),
plus one appended to `tests/test_aider_engine.py` (dispatch-chain wiring),
run together with the pre-existing `test_aider_engine.py`/`test_byok.py`
suite (22) — `pytest tests/test_droid_engine.py tests/test_pi_engine.py
tests/test_pi_engine_dispatch.py tests/test_engine_doctor.py
tests/test_aider_engine.py tests/test_byok.py -q` → 37 passed.

## Manual verification (isolated CCC_ALLOW_DUPLICATE_REPO=1 PORT=8099)
Spun up a second instance (not the shared dashboard) with fake `droid`/`aider`
CLI scripts on `CCC_DROID_BIN`/`CCC_AIDER_BIN`, against a scratch git repo:
- `GET /api/engines/doctor` — real payload, all 13 engines present, droid/
  aider/pi correctly `byok_ready: true`.
- `GET /api/engines/models` — `catalog.<engine>.byok_ready` correct for
  droid/aider/pi/kilo/opencode (true) vs claude (false).
- `ccc doctor --engine droid` / `ccc models --engine aider` — CLI output
  matches the API.
- `POST /api/sessions/spawn` engine=droid — real subprocess launched,
  `ARGV: exec --auto high --model claude-sonnet-5 --reasoning-effort high
  say hi`.
- Set a real BYOK profile (`w33verify`/openai) via `POST /api/byok/keys`
  (Keychain-backed), re-spawned droid with `key_profile: w33verify` — fake
  script's stdout captured `ENV:sk-test-XXXX`, proving the full round trip
  from Keychain through `byok_spawn_env` into the subprocess env. Deleted
  the test key afterward (`POST /api/byok/keys/delete` + confirmed gone from
  `security find-generic-password`).
- `POST /api/sessions/spawn` engine=aider — dispatched, real subprocess.
- `POST /api/sessions/spawn` engine=pi — clean `pi_unavailable`, no
  subprocess (CLI absent on this box, as expected).
- Isolated instance killed afterward; shared dashboard was never touched.

## Restart matrix
`server.py`, `ccc_server/{aider,droid,pi,engines}.py`, and `./ccc` all
changed. Per CLAUDE.md this is **Dashboard Y / Worker Y** — but per the
sprint brief's explicit instruction, restart is deliberately deferred to the
dispatching session, not performed here (same as W2-2).

## Scope discipline
Did not touch `ccc_server/ask.py` (reserved for a concurrent memory lane).
Two other files (`ccc_server/engines.py`, `server.py`) had concurrent,
unrelated sibling-session edits mixed into the working tree at commit time
(an Antigravity archive-signature fix). Isolated via `git diff` hunk
inspection + `git apply --cached` on a filtered patch (excluding the two
non-W3-3 hunks) + a bare `git commit`, rather than `git commit --only
<path>` — `--only` commits the whole working-tree file and would have swept
the sibling's unstaged hunks into this lane's commit. Verified via
`python3 -c "import server"` + the full test run post-split.

## Commits
- `3393fdfa` — chore(sprint): W3-3 scope-planning doc
- `dcaf1d34` — feat(byok): wire droid/aider/pi spawn paths, doctor, byok-ready markers
- `039c25a1` — docs(byok): document droid/aider/pi BYOK wiring and ccc doctor
