# W8-2 Release Notes Deterministic Pass

Status: complete

Summary:
- Reworked `scripts/release_notes.py` so deterministic release notes group
  commits into capability clusters by bucket, scope, and shared subject terms.
- Replaced generic fallback "why it matters" text with a curated scope/keyword
  outcome map; groups with no deterministic outcome move to Under the hood.
- Added Removed output, capped headline sections at 12 capability bullets, and
  regenerated `docs/release-notes/2026-09-03.md` and `.html` over the 7-day
  range without `--llm`.

Verification:
- `python3 -m pytest tests/test_release_notes.py` passes.
- `python3 -m pytest tests/test_release_notes.py tests/test_smoke.py` was
  attempted but hit the command timeout before completion.
- No CCC server, browser, or background process was started.
